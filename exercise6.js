function double(n) {
    console.log(n * 2);
}

// test line - this should print 20, but it won't yet:
console.log(double(5) + 10);

//the correction after debbuging
 
function double(n) {
    console.log(n * 2);
}
console.log(double(5) + 10);
